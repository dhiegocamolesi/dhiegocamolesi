CREATE TABLE `matviews` (
  `mv` varchar(64) NOT NULL,
  `view` varchar(64) NOT NULL,
  `last_refresh` timestamp NULL DEFAULT NULL,
  `refresh_time` int(11) DEFAULT NULL,
  PRIMARY KEY (`mv`)
) ENGINE=InnoDB DEFAULT CHARSET=latin1;
